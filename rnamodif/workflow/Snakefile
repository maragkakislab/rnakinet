from pathlib import Path

configfile: "config.yml"

include: "rules/basecalling.smk"
include: "rules/alignment.smk"
include: "rules/data_split.smk"
include: "rules/inference.smk"


rule all:
    input:
        # expand("outputs/splits/{target}/FAST5_{split}_SPLIT_DONE.txt", target=config['TARGET'], split=['train', 'validation','test'])
        expand(
            "outputs/predictions_limited/{model}/{target}/mean_pooling.pickle", 
            target=config['TARGET'], 
            model=config['INFERENCE_MODELS']
        )
        
        
        


# snakemake --cores 1 --rerun-incomplete --use-conda